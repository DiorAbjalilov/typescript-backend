import { Request, Response } from 'express';

// get all basic menus
const getBasicMenu = async (req: Request, res: Response) => {
  try {
  } catch (error) {
    console.log(error);
  }
};

// add new basic menu
const addBasicMenu = async (req: Request, res: Response) => {
  try {
  } catch (error) {
    console.log(error);
  }
};

// update old basic menu
const putBasicMenu = async (req: Request, res: Response) => {
  try {
  } catch (error) {
    console.log(error);
  }
};

// delete one basic menu
const deleteBasicMenu = async (req: Request, res: Response) => {
  try {
  } catch (error) {
    console.log(error);
  }
};

export const basicMenu = {
  getBasicMenu,
  addBasicMenu,
  putBasicMenu,
  deleteBasicMenu
};
