import { Request, Response } from 'express';

// get all sub menus
const getSubMenu = async (req: Request, res: Response) => {
  try {
  } catch (error) {
    console.log(error);
  }
};

// add new sub menu
const addSubMenu = async (req: Request, res: Response) => {
  try {
  } catch (error) {
    console.log(error);
  }
};

// update old sub menu
const putSubMenu = async (req: Request, res: Response) => {
  try {
  } catch (error) {
    console.log(error);
  }
};

// delete one sub menu
const deleteSubMenu = async (req: Request, res: Response) => {
  try {
  } catch (error) {
    console.log(error);
  }
};

export const subMenu = {
  getSubMenu,
  addSubMenu,
  putSubMenu,
  deleteSubMenu
};
